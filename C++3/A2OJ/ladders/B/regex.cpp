




#include <bits/stdc++.h>
#define db(x) cout << __LINE__ << "->" \
                   << " " << #x << "\t" << x << "\t\n"
using namespace std;
signed main()
{
    regex rg("[a-z]*([a-z]-[a-z])?[a-z]*[!,.]?");
    stringstream ss(s);
    string word;
    int count = 0;

    while (ss >> word)
    {
        if (word1 = "" and regex_match(word, rg))
        {
            count++;
        }
    }

    cout << regex_match(s, rg);
    return 0;
}