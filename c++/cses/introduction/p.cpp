#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
  // ios_base::sync_with_stdio(false);
  // cin.tie(NULL);
  // int i;

  // cin >> i;
  // cout << i;
  // /* code */
  // cerr << "time taken : " << (float)clock() / CLOCKS_PER_SEC << " secs" << endl;
  
  set <int>s;
  int a[]={1,4,42,2,4,2,4,24};
  s=a[];
  return 0;
}
