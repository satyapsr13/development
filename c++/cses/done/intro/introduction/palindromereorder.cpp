#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int j;
    string s;
    char a;
    cin >> s;
    j = s.size();
    for (int i = 0; s[i]; ++i)
    {
        if (s[i] == s[j])
        {
        }
        else{
            
        }
    }

    return 0;
}
