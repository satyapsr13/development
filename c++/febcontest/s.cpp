#include <bits/stdc++.h>
using namespace std;
#define watch(x) cout << (#x) << " is " << (x) << endl
// void swap(int &x,int &y){
//     x=x^y,y=x^y,x=x^y;
// }
int main()
{
    // int i = 7;
    // watch(7);
  // ios_base::sync_with_stdio(false);
  // cout<<4;

    // string s, a;
    // cin >> s>>a;
    // s+=a;
    // int a=4,b=6;
    // swap(a,b);
    // cout<<a<<b;
    // int i,m=5;
    // i=m^1;
    // cout<<i;
    // a = int (s[0]) * 2;
    // cout << stoi(s)*2;//we have function stoi() to convert string to integers

    return 0;
}
