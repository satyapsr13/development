#include<bits/stdc++.h>
using namespace std;
#define lp(i, a, b) for (int i = a; i <= b; i++)
int main(int argc, char const *argv[])
{
    map<char,int> mp1;
    mp1.insert(pair<char,int>(9,'a'));
    map<char,int>::iterator itr;
    lp()
    return 0;
}
